/*
 * Class declaration
 */
class Test extends Base {

    int x;
    bool check;

    public int ComputeFac(int num, char redundant){
        int num_aux;

        while (num < 1) {
                        
        }

        if (num < 1)
            num_aux = 1;
        else
            num_aux = num * (this.ComputeFac(num-1));
        return num_aux;
    }

    public void main(int[] a){
        io.println(new Test().ComputeFac(10));   //assuming the existence
                                                // of the classfile io.class

        return A;                                            
    }
}